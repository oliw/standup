{{#if isEditing}}
<input 
  placeholder="Entry" 
  escape-press="escape" 
  value={{formattedValue}}
  onInput={{action 'handleChange'}}
  disabled={{isNotEditing}} />
{{else}}
<p class="label" tabindex={{tabIndex}}>{{formattedValue}}</p>
{{/if}}
